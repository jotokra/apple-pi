{
  "_applepi_seed": true,
  "_comment": "INTERNAL ONBOARDING SCAFFOLDING — apple-pi writes this during P1. It is NOT the user's config. P3 (self-improvement) rewrites this file into a clean, model-tuned user config and REMOVES this marker + every _comment/_*_comment field. After P3, this file should read like the user wrote it for their model, with zero apple-pi scaffolding left. See .docs/PLAN.md D1.",
  "defaultProvider": "__APPLEPI_PROVIDER__",
  "defaultModel": "__APPLEPI_MODEL__",
  "defaultThinkingLevel": "high",
  "_thinking_comment": "SEED PLACEHOLDER. P3 reads the model's thinkingLevelMap from the pi-ai catalog and rewrites this to the model's real top tier; this comment is removed.",
  "hideThinkingBlock": false,
  "theme": "dark",
  "quietStartup": false,
  "defaultProjectTrust": "never",
  "collapseChangelog": true,
  "doubleEscapeAction": "tree",
  "treeFilterMode": "no-tools",
  "editorPaddingX": 1,
  "autocompleteMaxVisible": 8,
  "_models": {
    "favorites": [
      "__APPLEPI_PROVIDER__/__APPLEPI_MODEL__"
    ]
  },
  "compaction": {
    "enabled": true,
    "reserveTokens": 16384,
    "keepRecentTokens": 64000
  },
  "extensions": [
    "__APPLEPI_EXT_SYSINFO__",
    "__APPLEPI_EXT_WEB__",
    "__APPLEPI_EXT_VOICE__"
  ],
  "skills": [
    "__APPLEPI_SKILLS_DIR__"
  ],
  "promptTemplates": [
    "__APPLEPI_PROMPTS_DIR__"
  ],
  "themes": [],
  "shellPath": "__APPLEPI_SHELL__",
  "sessionDir": "__APPLEPI_SESSIONS_DIR__",
  "tools": {
    "allow": [
      "read", "write", "edit", "bash", "grep", "find", "ls",
      "web_search", "web_fetch",
      "browser_navigate", "browser_snapshot", "browser_click", "browser_type",
      "browser_set_checkbox", "browser_select_option", "browser_press_key",
      "browser_hover", "browser_wait", "browser_screenshot", "browser_eval",
      "browser_tabs", "browser_close"
    ]
  }
}
